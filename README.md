# Auto-bot

Transformer-style robot ↔ jet, built in Blender with Python and viewed on the web with Three.js.

```bash
npm install
npm run dev
```
Open the printed URL, click once to enable sound, use the buttons or scrubber.

See `CLAUDE.md` for the full pipeline, file map and design rules.
